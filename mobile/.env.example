# SenseiClaw — the Socratic tutor backend (chat, curriculum, practice questions).
# Defaults to http://167.86.98.204:4050 when unset.
EXPO_PUBLIC_SENSEI_API_URL=http://167.86.98.204:4050

# Optional: legacy REST backend (profile / preferences / mock tests).
# Sensei ships without it — leave unset unless you are running one.
# EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
