import type { ThemeDefinition } from '../tokens';
// eslint-disable-next-line @typescript-eslint/no-require-imports
const { themes } = require('./data');

export default themes['focus-emerald'] as ThemeDefinition;
